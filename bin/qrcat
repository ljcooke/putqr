#!/usr/bin/env ruby
# frozen_string_literal: true

require 'qrcat'

if ARGV.any?
  content = ARGV.first
  STDERR.puts 'warning: only encoding first argument' if ARGV.size > 1
else
  content = STDIN.read
end

puts QRCat::QRCode.new(content).render
